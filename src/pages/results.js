import { useAppContext } from '@/Context'
import React from 'react'

export default function results() {
  return (
    <div className='flex flex-col gap-5'>
        
        {/* {results[0].emotions.map((result)=> <div>{result.name}: {result.score *100}</div>)} */}
    </div>
  )
}
